# Copy this file to .env and adjust if needed.
# Chat API base URL (no trailing slash). Leave unset to use proxy (dev) / Vercel rewrites (prod).
# For Vercel: leave unset so /api/* is proxied via vercel.json (avoids mixed-content block).
# Or set to an HTTPS URL if your API is served over HTTPS.
# REACT_APP_API_BASE_URL=http://103.20.90.115

# Avoid "allowedHosts[0] should be a non-empty string" when running the dev server (e.g. with proxy)
DANGEROUSLY_DISABLE_HOST_CHECK=true
